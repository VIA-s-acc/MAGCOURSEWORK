\chapter{Алгоритм построения Парето-границы}\label{ch:algorithm}

% TODO (M5): ~5 страниц.
% Структура:
%   5.1 Многокритериальная постановка
%       min_u (E(u), G(u), τ(u)) при ограничениях из §4.
%       Множество недоминируемых решений (Парето-фронт).
%   5.2 ε-constraint метод (классика, см. Васильев гл. 8)
%       Фиксируем G ≤ ε_G, τ ≤ ε_τ → задача minE(u),
%       решаем дискретным PMP из §4 → получаем точку Парето.
%       Перебор ε по сетке → весь фронт.
%   5.3 Свойства Парето-фронта
%       — монотонность по числу фаз;
%       — структурное доминирование (нижняя оценка через charge balance).
%   5.4 Псевдокод и оценки сложности
%       — внешний цикл: O(N_ε²) по двумерной сетке ε;
%       — внутренний (PMP): O(K · |U|) на итерацию;
%       — overall: полиномиально по K и |U|.
%   5.5 Сравнение с NSGA-II (Deb 2002)
%       — NSGA-II работает «вслепую» (без структурной теоремы);
%       — наш алгоритм использует теорему 4.1 → меньше точек кандидатов;
%       — ожидаемое ускорение и его теоретическая оценка.

\section*{Заглушка}

Раздел будет наполнен на этапе~M5 в соответствии с Chapter Plan
(см. \texttt{.ai-factory/RESEARCH.md}).
